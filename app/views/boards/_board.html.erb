<% game_over = room.game_over? %>
<div id="<%= dom_id(room) %>" class="board board" data-gameover="<%=game_over%>">
  <%# current_user.id %>
  <%# room.active_player %>
  <%= render 'rooms/state', room: room, game_over: game_over %>
</div>