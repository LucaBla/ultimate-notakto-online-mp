<% game_over = room.game_over? %>
<div id="<%= dom_id(room) %>" class="board board" data-gameover="<%=game_over%>" data-active-player="<%=room.active_player%>">
  <%# room.active_player %>
  <%= render 'rooms/state', room: room, game_over: game_over %>
</div>